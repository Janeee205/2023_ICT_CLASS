우선 버전 확인한다.
  node --version
  -> v18.16.1

  npm --version
  -> 9.5.1
    
npx create-react-app react-basic

설치 끝나면
npm start

app.js가 메인

ctrl + , 로 설정 열어서
include Languages 검색
key : value 추가
javascript : javascriptreact


git에서 다른사람의 react 코드를 받았는데 node modules 없으면
 npm install 로 node modules 폴더 생성한다