export const add = (number) => {
  return number + number
}

export const multiply = (number) => {
  return number * 2
}

export const double = (number) => {
  return number * number
}

// const 로 쓸때는 default는 밑에 따로 적는다.
export default add;